rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    match /assets/{assetId} {
      allow read: if isSignedIn();
      allow write: if false; 
    }

    match /assets/{assetId}/intercepts/{interceptId} {
      allow read: if isSignedIn();
      allow write: if false;
    }
  }
}
